const counterNumbers = (numbers:number[]) => {
  const positiveNumbers = numbers.filter((number) => number > 0);
  const negativeNumbers = numbers.filter((number) => number < 0);
};

export default counterNumbers;
